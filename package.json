{
  "name": "pom_healthrecoversol",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "cypress": "^7.4.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "chrome:Script": "npx cypress run --browser chrome",
    "edge:Script": "npx cypress run --browser edge",
    "both:Scripts": "npm run edge:Script && npm run chrome:Script"
  },
  "author": "sumit",
  "license": "ISC"
}